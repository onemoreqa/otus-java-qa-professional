- job-template:
    name: 'runner-tests'
    description: 'Running autotests'
    project-type: pipeline
    concurrent: true
    logrotate:
      daysToKeep: 30
      numToKeep: 10
    properties:
      - build-discarder:
          num-to-keep: 10
    parameters:
      - text:
          name: YAML_CONFIG
          default: |
            BASE_URL: https://otus.ru
            BROWSER: chrome
            BROWSER_VERSION: 121.0
            GRID_URL: http://0.0.0.0/wd/hub
            TEST_VERSION: 0.0.1
            TEST_TYPES:
              - api
              - ui
              - mobile
      - string:
          name: BRANCH
          default: hw-7
          description: "Branch"
    pipeline-scm:
      scm:
        - git-runner
      script-path: jenkins/runner-tests.groovy


