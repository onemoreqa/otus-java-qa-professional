- job-template:
    name: jobs_uploader
    description: Jobs uploader to Jenkins
    project-type: pipeline
    parameters:
      - string:
          name: BRANCH
          default: main
          description: Branch
    pipeline-scm:
      scm:
        - git-runner
      script-path: jenkins/runner-tests.groovy